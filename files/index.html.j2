<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>EC2 Instance</title>
  <meta name="color-scheme" content="light" />
  <style>
    :root{
      /* Light mode only */
      --bg:#f4f7fa; --card:#ffffff; --muted:#475569; --text:#0b1220;
      --accent:#2563eb; --accent-2:#9333ea; --ok:#16a34a;
      --mono: ui-monospace, Menlo, Consolas, "Courier New", monospace;
      --sans: system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Arial, sans-serif;
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0; font-family:var(--sans); color:var(--text);
      background:
        radial-gradient(800px 400px at 10% 10%, rgba(37,99,235,.1), transparent 60%),
        radial-gradient(600px 300px at 90% 80%, rgba(147,51,234,.1), transparent 60%),
        var(--bg);
      display:grid; place-items:center; padding:24px;
    }
    .wrap{width:min(720px,100%)}
    .card{
      background: var(--card);
      border-radius:18px;
      padding:32px 28px;
      box-shadow:0 8px 20px rgba(0,0,0,.08);
      border:1px solid rgba(0,0,0,.05);
    }
    header{display:flex; align-items:center; justify-content:space-between; margin-bottom:20px}
    .title{display:flex; align-items:center; gap:12px}
    .dot{
      width:8px;height:8px;border-radius:50%;
      background:linear-gradient(135deg,var(--ok),var(--ok));
      box-shadow:0 0 10px rgba(22,163,74,.6);
      animation: blink 2s ease-in-out infinite;
    }
    @keyframes blink {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.3; }
    }
    h1{margin:0; font-size:clamp(22px,2.6vw,30px)}
    p.sub{margin:2px 0 0; color:var(--muted); font-size:14px}
    .badge{
      font-size:12px; padding:6px 10px; border-radius:999px;
      background:rgba(22,163,74,.1); color:var(--ok);
      border:1px solid rgba(22,163,74,.3);
      font-weight:700; text-transform:uppercase;
    }
    .grid{display:grid; grid-template-columns:1fr; gap:16px}
    .panel{
      background:rgba(0,0,0,.02);
      border:1px solid rgba(0,0,0,.05);
      border-radius:14px;
      padding:18px;
    }
    .label{
      font-size:12px;
      color:var(--muted);
      text-transform:uppercase;
      margin-bottom:6px;
      letter-spacing:.04em;
    }
    .value{
      font-family:var(--mono);
      font-size:clamp(16px,2.2vw,20px);
      color:var(--accent);
      word-break:break-word;
    }
    footer{
      margin-top:22px;
      color:var(--muted);
      font-size:13px;
      text-align:center;
    }
  </style>
</head>
<body>
  <main class="wrap">
    <section class="card">
      <header>
        <div class="title">
          <span class="dot" aria-hidden="true"></span>
          <div>
            <h1>Hello from EC2</h1>
            <p class="sub">Served directly by this instance</p>
          </div>
        </div>
        <span class="badge">Healthy</span>
      </header>
      <div class="grid">
        <div class="panel">
          <div class="label">Hostname</div>
          <div class="value">{{ ansible_hostname | default(inventory_hostname | default('unknown')) }}</div>
        </div>
      </div>
      <footer>Behind an Application Load Balancer</footer>
    </section>
  </main>
</body>
</html>